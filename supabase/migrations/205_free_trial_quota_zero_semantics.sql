COMMENT ON COLUMN public.question_banks.free_trial_block_limit IS
'Lifetime trial block quota. Zero means the bank is configured as trial-visible but no new trial blocks may be created; positive values allow that many blocks.';
