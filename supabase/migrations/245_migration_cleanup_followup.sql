-- Before first production launch, squash/renumber this exploratory hardening chain
-- into a clean reproducible baseline after disposable-database verification. Git
-- history retains the implementation trail; production migrations should stay small
-- and intentional.
SELECT 1;
