-- This project is pre-production with no important user data. These migrations favor
-- correcting authorization/state invariants now rather than preserving accidental
-- legacy behavior. Validate on a disposable database before merge.
SELECT 1;
