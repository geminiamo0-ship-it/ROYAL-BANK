export function QuestionBankPanelHeader({ title }: { title: string }) {
  return (
    <div className="px-[12px] py-[9px] text-[11px] uppercase tracking-[0.25px] text-[#4f565b]">
      {title}
    </div>
  );
}
