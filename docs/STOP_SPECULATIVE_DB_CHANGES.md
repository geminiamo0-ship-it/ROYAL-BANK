# Stop Speculative Database Changes

The target model is sufficiently defined for this checkpoint. Do not add more schema/RPC changes until the existing application paths and current session-creation SQL are inspected and integrated. Future database changes should be driven by a verified application requirement or failing regression test.
