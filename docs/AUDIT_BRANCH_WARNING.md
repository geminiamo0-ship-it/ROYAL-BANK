# Audit Branch Warning

`audit/core-hardening` is an implementation laboratory, not a deploy target. It contains intentionally verbose exploratory SQL that will be consolidated. Do not point production/Cloudflare deployment at this branch until merge gates pass and the migration chain is cleaned.
