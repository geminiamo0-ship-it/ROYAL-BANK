# Runtime Validation Target

Use runtime validation at application trust boundaries for bank/session/question/option IDs, question count, difficulties, categories/topics, session mode, and selection mode. Zod is already a project dependency. Database constraints/RPC authorization remain the final enforcement layer.
