# Core Architecture Target

Keep security/business/data logic in server/database layers and interactive presentation in client components. Refactor completed core by responsibility, not by arbitrary file-size targets. Avoid overengineering: fewer clear modules and explicit database boundaries are preferred over many thin abstractions.
