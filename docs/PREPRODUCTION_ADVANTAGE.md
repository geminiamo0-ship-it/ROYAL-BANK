# Pre-Production Cleanup Strategy

Because the site has no important real user data yet, this is the best point to correct schema/access/state design and establish a clean migration baseline. Avoid compatibility hacks whose only purpose is preserving accidental development data. Keep behavior that is intentional; remove accidental implementation debt.
