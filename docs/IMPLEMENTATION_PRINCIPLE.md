# Implementation Principle

Preserve intentional product behavior, remove accidental complexity. Database authorization/state invariants should be explicit and testable; application code should orchestrate them rather than duplicate them. Do not add architecture layers or caches without a measured need.
